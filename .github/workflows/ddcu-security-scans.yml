name: Security Scans

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scan:
    permissions:
      contents: read
      security-events: write
    uses: LukeEvansTech/shared-workflows/.github/workflows/security-scans.yml@7f10cdc73446f1d0adbdf12074dcd0f961a121fa # v1
    with:
      checkov-frameworks: github_actions
      trivy-scanners: vuln,secret,misconfig
